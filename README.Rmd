--- 
output: github_document
---

<a href="http://www.repostatus.org/#wip"><img src="http://www.repostatus.org/badges/latest/wip.svg" alt="Project Status: WIP – Initial development is in progress, but there has not yet been a stable, usable release suitable for the public." /></a>


```{r setup, include=FALSE}
library(fs)
library(tidyverse)
library(glue)
knitr::opts_chunk$set(echo = TRUE)
```

## datasets

This repository contains the following datasets:

```{r datasets-list, echo=FALSE, results='asis'}

directories <- dir_ls(type = "directory") %>% as.list() 

urls <- directories %>% glue_data("https://github.com/tiernanmartin/datasets/tree/master/{.}")

directories %>% glue_data("- [ ] [`{.}`]({urls}) \n")

```

